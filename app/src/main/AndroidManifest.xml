<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Recordatorios de rutinas (Android 13+) -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Descarga de modelos de IA en segundo plano -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- Aviso de "sin conexión" en el panel (la trae Firebase, pero mejor declararla) -->
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <!-- Visibilidad de paquetes (Android 11+): sin esto, queryIntentActivities no ve el
         reconocedor de voz y el botón de dictado se esconde en dispositivos que SÍ lo tienen. -->
    <queries>
        <intent>
            <action android:name="android.speech.action.RECOGNIZE_SPEECH" />
        </intent>
        <intent>
            <action android:name="android.speech.RecognitionService" />
        </intent>
    </queries>

    <application
        android:name=".HabitlyApp"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:largeHeap="true"
        android:theme="@style/Theme.Habitly">

        <!-- LiteRT-LM GPU backend support -->
        <uses-native-library android:name="libvndksupport.so" android:required="false"/>
        <uses-native-library android:name="libOpenCL.so" android:required="false"/>
        <!-- adjustResize: sin él, el sistema panea la ventana entera al abrir el teclado
             (la cabecera se va tras la barra de estado) en vez de dejar que Compose
             gestione el hueco con los insets del IME. -->
        <!-- singleTask: la app es de una sola Activity, y con "Compartir con Habitly" hace
             falta que un ACTION_SEND entre por onNewIntent en la instancia que ya existe en
             vez de apilar una segunda copia con su propio estado. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:theme="@style/Theme.Habitly"
            android:windowSoftInputMode="adjustResize"
            android:configChanges="uiMode">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- Compartir con Habitly: texto desde el navegador, notas, WhatsApp… El texto se
                 procesa EN EL DISPOSITIVO con el modelo local; nunca sale del teléfono. -->
            <intent-filter>
                <action android:name="android.intent.action.SEND" />

                <category android:name="android.intent.category.DEFAULT" />

                <data android:mimeType="text/plain" />
            </intent-filter>

            <!-- Atajos del icono del launcher (mantener pulsado). -->
            <meta-data
                android:name="android.app.shortcuts"
                android:resource="@xml/shortcuts" />
        </activity>

        <!-- Con targetSdk >= 34 el tipo de foreground service debe ir declarado en el
             <service>; el manifest de WorkManager no lo trae, así que se fusiona aquí
             para que ModelDownloadWorker pueda llamar a setForeground con DATA_SYNC. -->
        <service
            android:name="androidx.work.impl.foreground.SystemForegroundService"
            android:foregroundServiceType="dataSync"
            tools:node="merge" />

        <!-- Widget de pantalla de inicio: lista de la compra y rutinas de hoy -->
        <receiver
            android:name=".feature.widget.HabitlyWidgetReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/habitly_widget_info" />
        </receiver>
    </application>

</manifest>