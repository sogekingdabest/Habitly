{
  "name": "habitly-firestore-rules",
  "private": true,
  "scripts": {
    "test:firestore-rules": "firebase emulators:exec --project demo-habitly-rules --only firestore \"node --test firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.1",
    "firebase": "12.17.1",
    "firebase-tools": "^15.27.0"
  }
}
